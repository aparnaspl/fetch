
<?xml version="1.0" encoding="UTF-8"?>
<project name="MyProject" default="phpcs">
    <property name="phpcs"   value="phpcs"/> 
  

     <target name="phpcs">
    <exec executable="cmd">
        <arg line="/c phpcs
            --report=checkstyle
            --ignore='*.xml'
            --standard='PHPCS'

            -p
            test.php " />
    </exec>
</target> 
    
    </exec>
</target>
</project>
